import React, { Component } from 'react';

class header extends Component {

  render = () => {
    return (
      <div id="shortform-header">
          <h1 className="header-title">Shortform</h1>
      </div>
    );
  }

}

export const Header = header;
